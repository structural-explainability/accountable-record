schema = "ar-element-type-1"

[identity]
canonical_uri = "https://structural-explainability.org/accountable-record/elements/subjects/subject-kind"
persistent_id = "urn:ar:se:accountable-record:subjects:subject-kind"
compact_id = "se.accountable-record.subjects.subject-kind"
local_name = "subject-kind"
label = "Subject Kind"

[namespace]
authority = "structural-explainability.org"
authority_alias = "se"
project = "accountable-record"

[classification]
artifact_kind = "element-type"
component_group = "subjects"

[release]
version = "0.1.0"
status = "working-draft"
immutable = false

[definition]
description = """
A declared kind or category for a record subject, owned by a profile, domain,
authority, external vocabulary, or mapping guide rather than by AR as a
universal ontology.
"""
plain_language = """
The kind of thing the record is about.
"""

[identity_rules]
element_type_id_field = "element_type_id"
element_instance_id_field = "element_instance_id"

[structure]
schema = "schema.schema.json"
checks = "checks.toml"
examples = "examples/"

[compatibility]
breaking_changes_require_new_major = true
requires = [
  "se.accountable-record.subjects.record-subject >=0.1.0",
]

[deprecation]
deprecated = false
successor = ""

[stewardship]
maintainer = "Structural Explainability"
status = "active"

[hosting]
provider = "github"
organization = "structural-explainability"
repository = "accountable-record"
url = "https://github.com/structural-explainability/accountable-record"
