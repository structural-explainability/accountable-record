schema = "ar-element-type-1"

[identity]
canonical_uri = "https://structural-explainability.org/accountable-record/elements/failure-modes/failure-mode"
persistent_id = "urn:ar:se:accountable-record:failure-modes:failure-mode"
compact_id = "se.accountable-record.failure-modes.failure-mode"
local_name = "failure-mode"
label = "Failure Mode"

[namespace]
authority = "structural-explainability.org"
authority_alias = "se"
project = "accountable-record"

[classification]
artifact_kind = "element-type"
component_group = "failure-modes"

[release]
version = "0.3.0"
status = "working-draft"
immutable = false

[definition]
description = """
A recurrent structural failure pattern that makes records harder to inspect,
verify, transform, compare, or preserve under disagreement.
"""
plain_language = """
A common way records become unreliable or hard to inspect.
"""

[identity_rules]
element_type_id_field = "element_type_id"
element_instance_id_field = "element_instance_id"

[structure]
schema = "schema.schema.json"
checks = "checks.toml"
examples = "examples/"

[compatibility]
breaking_changes_require_new_major = true
requires = [
  "se.accountable-record.failure-modes.structural-collapse >=0.1.0",
  "se.accountable-record.failure-modes.collapse-pattern >=0.1.0",
]

[deprecation]
deprecated = false
successor = ""

[stewardship]
maintainer = "Structural Explainability"
status = "active"

[hosting]
provider = "github"
organization = "structural-explainability"
repository = "accountable-record"
url = "https://github.com/structural-explainability/accountable-record"
