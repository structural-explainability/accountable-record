schema = "ar-element-type-1"

[identity]
canonical_uri = "https://structural-explainability.org/accountable-record/elements/transformations/transformation-kind"
persistent_id = "urn:ar:se:accountable-record:transformations:transformation-kind"
compact_id = "se.accountable-record.transformations.transformation-kind"
local_name = "transformation-kind"
label = "Transformation Kind"

[namespace]
authority = "structural-explainability.org"
authority_alias = "se"
project = "accountable-record"

[classification]
artifact_kind = "element-type"
component_group = "transformations"

[release]
version = "0.3.0"
status = "working-draft"
immutable = false

[definition]
description = """
A profile-defined label for the domain-specific kind of transformation being
declared, such as amendment, merger, migration, correction, reclassification,
renaming, re-encoding, or other profile-recognized change type.
"""
plain_language = """
The domain-specific name for a declared transformation.
"""

[identity_rules]
element_type_id_field = "element_type_id"
element_instance_id_field = "element_instance_id"

[structure]
schema = "schema.schema.json"
checks = "checks.toml"
examples = "examples/"

[compatibility]
breaking_changes_require_new_major = true
requires = [
  "se.accountable-record.conformance.conformance-profile >=0.1.0",
]

[deprecation]
deprecated = false
successor = ""

[stewardship]
maintainer = "Structural Explainability"
status = "active"

[hosting]
provider = "github"
organization = "structural-explainability"
repository = "accountable-record"
url = "https://github.com/structural-explainability/accountable-record"
