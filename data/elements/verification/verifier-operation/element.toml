schema = "ar-element-type-1"

[identity]
canonical_uri = "https://structural-explainability.org/accountable-record/elements/verification/verifier-operation"
persistent_id = "urn:ar:se:accountable-record:verification:verifier-operation"
compact_id = "se.accountable-record.verification.verifier-operation"
local_name = "verifier-operation"
label = "Verifier Operation"

[namespace]
authority = "structural-explainability.org"
authority_alias = "se"
project = "accountable-record"

[classification]
artifact_kind = "element-type"
component_group = "verification"

[release]
version = "0.1.0"
status = "working-draft"
immutable = false

[definition]
description = """
A declared verifier operation, such as inspection or validation, that describes
whether the verifier summarizes structure, runs claims, produces an AR report,
or performs another contract-recognized operation.
"""
plain_language = """
A named action a verifier can perform, such as inspect or validate.
"""

[identity_rules]
element_type_id_field = "element_type_id"
element_instance_id_field = "element_instance_id"

[structure]
schema = "schema.schema.json"
checks = "checks.toml"
examples = "examples/"

[compatibility]
breaking_changes_require_new_major = true
requires = [
  "se.accountable-record.verification.verifier >=0.1.0",
  "se.accountable-record.verification.verifier-expectation >=0.1.0",
]

[deprecation]
deprecated = false
successor = ""

[stewardship]
maintainer = "Structural Explainability"
status = "active"

[hosting]
provider = "github"
organization = "structural-explainability"
repository = "accountable-record"
url = "https://github.com/structural-explainability/accountable-record"
